<%= link_to "今月の予算を設定", "/budgets/new", class:"btn btn-primary", id:"create-new-budget" %>
<h3> 【今月の設定予算】 </h3>
<br>
<% @budgets.each do |budget| %> 
  <p>・<%= budget.food_expenses %></p>
  <p>・<%= budget.dining_expenses %></p>
  <p>・<%= budget.daily_necessities %></p>
<% end %>

<div class="container">
  <div class="row">
    <div class="col order-last">
      日用品
    </div>
    <div class="col">
      外食費
    </div>
    <div class="col order-first">
      食費
    </div>
  </div>
</div>

